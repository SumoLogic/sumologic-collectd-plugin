language: python

# install collectd
before_install:
  - sudo sudo apt-get install collectd

# command to install dependencies
install: "pip install -r requirements.txt"

# command to run tests
script:
  py.test -v test/ --cov=src/

after_success:
  - codecov
